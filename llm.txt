# ASCII-art CLI — custom agents demo

> File map for coding agents: every file in this branch and what it is for.

```text
.
├── .github/
│   ├── agents/
│   │   ├── implementer.md   # Write-capable agent: reads PLAN.md and builds the CLI.
│   │   └── planner.md        # Read-only agent: researches via Exa MCP, writes PLAN.md.
│   └── skills/
│       └── ascii-art/
│           ├── SKILL.md     # How each style renders; the implementer reads it first.
│           └── examples/
│               ├── fire-example.txt    # One rendered frame of the fire style.
│               └── matrix-example.txt  # One rendered frame of the matrix style.
├── .vscode/
│   └── mcp.json     # Exa MCP server config (web search/fetch) used by the planner.
├── AGENTS.md        # Goal, runtime, CLI contract, and the two-agent workflow.
├── PROMPT.md        # Demo script: out-of-the-box agents first, then the custom ones.
├── README.md        # One-line project intro.
└── llm.txt          # This file map.
```
